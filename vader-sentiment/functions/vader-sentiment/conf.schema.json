{
  "type": "object",
  "title": "",
  "required": ["inputField", "outputField"],
  "properties": {
    "inputField": {
      "type": "string",
      "title": "Input Field",
      "default": "comment",
      "description": "The name of the event field that contains text to run sentiment analysis with."
    },
    "outputField": {
      "type": "string",
      "title": "Output Field",
      "default": "sentiment_score",
      "description": "The name of the output field to assign sentiment score (-1 to 1) to."
    }
  }
}
